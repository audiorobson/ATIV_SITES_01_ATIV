# Registro de evidências

`claim-registry.csv` é a fonte obrigatória para números, clientes, contratos, certificações, fabricantes, SLAs e resultados publicados.

Enquanto um claim não possuir `claim_id`, fonte verificável, responsável e status `approved`, ele não pode ser promovido de `content/inbox/` para uma página publicável.
