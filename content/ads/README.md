# Conteúdo de mídia paga

Landing pages aprovadas ficam nesta pasta e devem usar rotas `/lp/**` previstas em `seo/route-plan.csv`.

Além do contrato editorial geral, o frontmatter deve declarar `page_type: ads_landing` e `robots: noindex,follow`. Essas páginas ficam fora do sitemap.
