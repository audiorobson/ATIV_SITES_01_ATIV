# Public base URL used only when an absolute URL is required.
# Local development may keep the default below. Production must provide its approved canonical origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
